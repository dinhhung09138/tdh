@using Utils;
@model IEnumerable<TDH.Model.ViewModel.WebSite.PostViewModel>
<div class="top-news">
    <h4 class="side">Xem nhiều</h4>
    <div class="top-inner">
        @{
            foreach (var item in Model)
            {
                <div class="top-text">
                    <a href="@item.Alias"><img src="@item.Image" data-src="@item.Image" class="lazy img-responsive" alt="@item.Title" title="@item.Title" /></a>
                    <h5 class="top"><a href="@item.Alias">@item.Title</a></h5>
                    <div class="td-post-date two"><i class="glyphicon glyphicon-time"></i>@item.CreateDate </div>
                </div>
            }
        }
    </div>
</div>
